- model_class = Attr3
.page-header
  %h1=t '.title', :default => model_class.model_name.human.titleize

%p
  %strong= model_class.human_attribute_name(:name) + ':'
  %br
  = @attr3.name
%p
  %strong= model_class.human_attribute_name(:tenant_id) + ':'
  %br
  = @attr3.tenant_id


= link_to t('.back', :default => t("helpers.links.back")), attr3s_path, :class => 'btn btn-default'
= link_to t('.edit', :default => t("helpers.links.edit")), edit_attr3_path(@attr3), :class => 'btn btn-default'
= link_to t('.destroy', :default => t("helpers.links.destroy")), attr3_path(@attr3), :method => "delete", :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-danger'
