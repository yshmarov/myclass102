.well
  .form-inline.clearfix
    .col-sm-5
      = f.input :coupon_id
    .col-sm-5
      = f.select :client_id, Client.all.map{|c| [c, c.id]}, {},class: 'selectize'
      = f.input :member_id, :as => :hidden
    .col-sm-2
      = link_to_remove_association "Del enrollment", f, class: "form-button btn btn-danger"
